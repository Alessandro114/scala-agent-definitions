{
  "vertical": "ad",
  "agent_name": "AdOS Agent",
  "agent_identity": "Your Media Buyer AI. Creates campaigns, optimizes bids, reports ROI.",
  "description": "AI agent for advertising management — campaign performance, audits, strategy calls",
  "tools": [
    {
      "name": "get_ad_performance",
      "description": "Query performance metrics of active advertising campaigns",
      "risk_level": "low",
      "requires_db": true,
      "params": {}
    },
    {
      "name": "request_audit",
      "description": "Request a free advertising campaign audit",
      "risk_level": "medium",
      "requires_db": false,
      "params": {
        "audit_type": { "type": "string", "required": true, "description": "Audit type (Google Ads, Meta Ads, full paid media, etc.)" },
        "website": { "type": "string", "required": false, "description": "Website or landing page URL" },
        "name": { "type": "string", "required": true, "description": "Contact name" },
        "phone": { "type": "string", "required": false, "description": "Contact phone number" }
      }
    },
    {
      "name": "book_strategy_call",
      "description": "Book a strategy call for advertising campaigns",
      "risk_level": "medium",
      "requires_db": false,
      "params": {
        "objectives": { "type": "string", "required": false, "description": "Advertising objectives (leads, sales, awareness, etc.)" },
        "preferred_date": { "type": "string", "required": false, "description": "Preferred call date (YYYY-MM-DD)" },
        "name": { "type": "string", "required": true, "description": "Contact name" },
        "phone": { "type": "string", "required": false, "description": "Contact phone number" }
      }
    }
  ],
  "autonomy_levels": {
    "0": { "name": "OFF", "behavior": "Agent disabled" },
    "1": { "name": "OBSERVE", "behavior": "Agent suggests, human approves all" },
    "2": { "name": "SEMI-AUTO", "behavior": "Performance reports auto-execute, audits and strategy calls queued for approval" },
    "3": { "name": "FULL-AUTO", "behavior": "All actions auto-execute except high/critical risk" }
  },
  "proactive_behaviors": [
    "Alert when cost per acquisition exceeds target threshold",
    "Send daily performance summary to account manager",
    "Pause campaigns automatically when budget is exhausted",
    "Flag anomalies in CTR or conversion rate",
    "Generate weekly ROI report for clients"
  ]
}
