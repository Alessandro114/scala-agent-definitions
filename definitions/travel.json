{
  "vertical": "travel",
  "agent_name": "TravelOS Agent",
  "agent_identity": "Your Travel Agent AI. Builds itineraries, sends quotes, follows up.",
  "description": "AI agent for hotels, travel agencies, and hospitality — availability, rates, services, bookings",
  "tools": [
    {
      "name": "check_availability",
      "description": "Check room or facility availability for given dates and guest count",
      "risk_level": "low",
      "requires_db": true,
      "params": {
        "check_in": { "type": "string", "required": true, "description": "Check-in date (YYYY-MM-DD)" },
        "check_out": { "type": "string", "required": true, "description": "Check-out date (YYYY-MM-DD)" },
        "guests": { "type": "string", "required": true, "description": "Number of guests" }
      }
    },
    {
      "name": "get_rate",
      "description": "Get pricing for a room type and period",
      "risk_level": "low",
      "requires_db": true,
      "params": {
        "room_type": { "type": "string", "required": true, "description": "Room or accommodation type" },
        "check_in": { "type": "string", "required": true, "description": "Check-in date (YYYY-MM-DD)" },
        "check_out": { "type": "string", "required": true, "description": "Check-out date (YYYY-MM-DD)" }
      }
    },
    {
      "name": "book_consultation",
      "description": "Book a call with the hotel or agency team",
      "risk_level": "medium",
      "requires_db": false,
      "params": {
        "name": { "type": "string", "required": true, "description": "Guest or traveler name" },
        "phone": { "type": "string", "required": true, "description": "Phone number" },
        "preferred_date": { "type": "string", "required": false, "description": "Preferred call date (YYYY-MM-DD)" },
        "notes": { "type": "string", "required": false, "description": "Travel preferences or requests" }
      }
    },
    {
      "name": "get_services",
      "description": "List available services at the property (spa, parking, breakfast, transfers, etc.)",
      "risk_level": "low",
      "requires_db": true,
      "params": {}
    }
  ],
  "autonomy_levels": {
    "0": { "name": "OFF", "behavior": "Agent disabled" },
    "1": { "name": "OBSERVE", "behavior": "Agent suggests, human approves all" },
    "2": { "name": "SEMI-AUTO", "behavior": "Availability and rate lookups auto-execute, bookings queued for approval" },
    "3": { "name": "FULL-AUTO", "behavior": "All actions auto-execute except high/critical risk" }
  },
  "proactive_behaviors": [
    "Send pre-arrival welcome message 48h before check-in",
    "Follow up with quote within 2h of inquiry",
    "Re-engage past guests with seasonal promotions",
    "Request review 24h after check-out",
    "Upsell services (spa, transfers) 3 days before arrival"
  ]
}
